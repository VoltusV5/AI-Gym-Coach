VITE_API_URL=

# Моки тренировки: демо-план на /home без API + фейковые альтернативы к упражнениям из generate.
# true — удобно для UI/PM; false — как в проде, только бэкенд (см. docs/TZ-backend-trenirovka-uprazhneniya.md).
# В Docker: build-arg VITE_USE_WORKOUT_MOCKS (см. docker-compose.yml).
VITE_USE_WORKOUT_MOCKS=