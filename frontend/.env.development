# Не задавайте URL — запросы идут через прокси Vite на 127.0.0.1:5050 (см. vite.config.js).
# Если нужен прямой вызов бэкенда: VITE_API_URL=http://localhost:5050
VITE_USE_MOCK=false

# Демо-план и альтернативы без API. Для интеграции с бэкендом поставьте false.
VITE_USE_WORKOUT_MOCKS=true
